# Offline dataset (no Kaggle)

After `git clone`, run:

```bash
python data/install_offline_dataset.py
```

This uses only files already in `data/offline_bundles/` (FracAtlas CC BY 4.0).

Citation: Abedeen et al., FracAtlas, Scientific Data / Figshare, https://doi.org/10.6084/m9.figshare.22363012
